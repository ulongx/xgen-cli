package {{packagename}}.model;

import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.Data;
import org.springframework.format.annotation.DateTimeFormat;
import java.math.BigDecimal;
import java.util.Date;

/**
 * {{tablecomment}}
 * Created by xgen-cli on {{currentdate}}.
 */
@Data
public class {{toUpCase tablename}}{

{{#rows}}
  {{#switch data_type}}
    {{#case 'varchar'}}
      //{{column_comment}}
      private String {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'char'}}
      //{{column_comment}}
      private String {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'int'}}
      //{{column_comment}}
      private Integer {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'bigint'}}
      //{{column_comment}}
      private Integer {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'date'}}
      //{{column_comment}}
      @DateTimeFormat(pattern = "yyyy-MM-dd")
      @JsonFormat(timezone = "GMT+8", pattern = "yyyy-MM-dd")
      private Date {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'timestamp'}}
      //{{column_comment}}
      @DateTimeFormat(pattern = "yyyy-MM-dd")
      @JsonFormat(timezone = "GMT+8", pattern = "yyyy-MM-dd")
      private Date {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'datetime'}}
      //{{column_comment}}
      @DateTimeFormat(pattern = "yyyy-MM-dd")
      @JsonFormat(timezone = "GMT+8", pattern = "yyyy-MM-dd")
      private Date {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'float'}}
      //{{column_comment}}
      private Float {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'double'}}
      //{{column_comment}}
      private Double {{toUpCaseF column_name}};
    {{/case}}
    {{#case 'decimal'}}
      //{{column_comment}}
      private BigDecimal {{toUpCaseF column_name}};
    {{/case}}
  {{/switch}}
{{/rows}}
}
